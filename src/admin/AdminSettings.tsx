import{useState}from'react';import{useCms}from'../cms/CmsContext';export default function AdminSettings(){const{cms,updateCms,resetCms}=useCms();const[x,setX]=useState(cms.settings);return <div><div className="qa-title"><p>CONFIGURATION</p><h1>Settings</h1><button onClick={()=>updateCms(c=>({...c,settings:x}))}>Save</button></div><div className="qa-card">{Object.entries(x).map(([k,v])=><label>{k}<input value={v} onChange={e=>setX({...x,[k]:e.target.value})}/></label>)}<button className="qa-danger" onClick={()=>confirm('Reset all BARCODE CMS content?')&&resetCms()}>Reset CMS content</button></div></div>}
