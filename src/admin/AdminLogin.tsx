import{useState,type FormEvent}from'react';import{Navigate,useLocation,useNavigate}from'react-router-dom';import{useAdminAuth}from'../cms/auth/AdminAuthContext';import logo from'../assets/logo/barcode-logo.png';
export default function AdminLogin(){const{admin,loading,login}=useAdminAuth();const nav=useNavigate();const loc=useLocation();const[e,setE]=useState('');const[p,setP]=useState('');const[err,setErr]=useState('');const[busy,setBusy]=useState(false);if(loading)return <div className="qa-login">Checking access…</div>;if(admin)return <Navigate to="/admin" replace/>;async function submit(x:FormEvent){x.preventDefault();setBusy(true);setErr('');try{await login(e,p);nav((loc.state as any)?.from||'/admin',{replace:true})}catch(z){setErr(z instanceof Error?z.message:'Unable to sign in')}finally{setBusy(false)}}return <main className="qa-login"><section className="qa-login-brand"><img src={logo}/><h1>Q - CMS</h1><small>by Qyasat</small><p>Private workspace for BARCODE content, enquiries, analytics and digital tracking.</p></section><form onSubmit={submit} className="qa-login-form"><p>SECURE ADMINISTRATOR ACCESS</p><h2>Sign in to BARCODE</h2><label>Email<input type="email" required value={e} onChange={x=>setE(x.target.value)}/></label><label>Password<input type="password" required value={p} onChange={x=>setP(x.target.value)}/></label>{err&&<div className="qa-error">{err}</div>}<button disabled={busy}>{busy?'Signing in…':'Enter CMS'}</button></form></main>}
