import {Coffee,CupSoda,GlassWater,Leaf,Croissant,Sandwich} from 'lucide-react';import {useCms} from '../cms/CmsContext';
const icons=[Coffee,CupSoda,GlassWater,Leaf,Croissant,Sandwich];
export function MenuSection(){const{cms}=useCms();const m=cms.home.menu;return <section id="menu" className="menu-section"><aside className="side-copy left">{m.leftSide.split('\n').map((x,i)=><span key={i}>{x}<br/></span>)}</aside><div className="menu-inner"><div className="eyebrow">{m.eyebrow}<i/></div><h2>{m.title}<br/><em>{m.accent}</em></h2><i className="short-rule"/><p className="menu-intro">{m.intro}</p><div className="menu-grid">{m.items.map((item,index)=>{const Icon=icons[index%icons.length];return <article className="menu-card" key={index}><div className="menu-symbol"><span className="number">{String(index+1).padStart(2,'0')}</span><i/><b><Icon size={31} strokeWidth={1.35}/></b></div><h3>{item.title}</h3><p>{item.desc}</p></article>})}</div></div><aside className="side-copy right">{m.rightSide.split('\n').map((x,i)=><span key={i}>{x}<br/></span>)}</aside></section>}
