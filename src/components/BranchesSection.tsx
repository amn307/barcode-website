import {useState} from 'react';import {useCms} from '../cms/CmsContext';
export function BranchesSection(){const{cms}=useCms();const b=cms.home.branches;const[active,setActive]=useState(0);const items=b.items;const branch=items[Math.min(active,Math.max(0,items.length-1))];if(!branch)return <section id="branches" className="branches-section"/>;const prev=()=>setActive(c=>c===0?items.length-1:c-1),next=()=>setActive(c=>c===items.length-1?0:c+1);return <section id="branches" className="branches-section"><div className="branches-intro"><div><div className="eyebrow">OUR BRANCHES <i/></div><h2>{b.title}</h2><p>{b.intro}</p></div><div className="branch-side-copy"><i/><span>GOOD<br/>COFFEE<br/>BETTER<br/>PEOPLE</span><b/></div></div><div className="branch-list">{items.map((x,i)=><button key={i} type="button" className={active===i?'active':''} onClick={()=>setActive(i)}><div className="branch-number"><span>{String(i+1).padStart(2,'0')}</span><b/></div><div className="branch-name-row"><strong>{x.name}</strong><i/></div></button>)}</div><div className="map-panel"><img src={b.map.src} alt={b.map.alt} className="branches-map-image"/></div><div className="branch-feature"><img key={branch.image.src} src={branch.image.src} alt={branch.image.alt} className="branch-feature-image"/><div className="branch-gradient"/><div className="branch-info"><h3>{branch.name}</h3><p><span className="location-icon">⌖</span><span>{branch.address}</span></p><a href="#">VIEW ON MAP <span>→</span></a></div><div className="branch-arrows"><button type="button" onClick={prev}>←</button><button type="button" onClick={next}>→</button></div></div><div className="branches-est">EST. 2019 <i/></div></section>}
